# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Lockfile is environment-specific — let CI generate it fresh
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Environment / secrets — never commit
.env
.env.local
.env.*.local

# Enrichment script cache — regenerable, persisted across CI runs via actions/cache
scripts/.cache
